#!/usr/bin/bash

if [ ! -e v0.1.4.tar.gz ]; then
	curl -O -L https://github.com/tensorflow/tensorboard/archive/0.1.4.tar.gz
fi	
rm -rf tensorboard-0.1.4
rm -rf tmp
mkdir tmp

tar -axf v1.3.0.tar.gz

pushd tensorflow-1.3.0

	bazel clean
	bazel build 


popd


